Read PLAN.md before every session.
Update PLAN.md status after completing each module.
Never hardcode API keys — use .env only.
Entry point is agent_orchestrator.py — do not rename.
Output CSV columns: status, product_area, response, 
justification, request_type.